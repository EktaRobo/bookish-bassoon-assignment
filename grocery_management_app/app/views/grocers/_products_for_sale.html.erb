<h4 class="order-selected-text"><h4>
<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <% @items.each do |item| %>
      <%= render "item_row", item: item, cart: @cart, order: @order %>
    <% end %>
  </div>
</div>
<% link_to "Go Back", root_path, class: "col-centered btn btn-primary" %>