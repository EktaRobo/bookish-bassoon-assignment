<h3 class="text-center">Items in order</h3>

<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <% @carts.each do |cart| %>
      <%= render "item_row", item: Item.find(cart.item_id), cart: cart, order: @order %>
    <% end %>
  </div>
</div>